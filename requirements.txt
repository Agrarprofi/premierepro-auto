# autoedit – Auto-Edit-Pipeline für Premiere Pro (macOS / Apple Silicon)
# Benötigt Python 3.11+ (auf macOS: brew install python@3.12)
fastapi>=0.110
uvicorn[standard]>=0.29
numpy>=1.26
scipy>=1.12
pyyaml>=6.0
ffmpeg-python>=0.2.0
anthropic>=0.40
python-dotenv>=1.0
python-multipart>=0.0.9

# Transkription (läuft lokal, lädt Modelle beim ersten Start)
whisperx>=3.1 ; sys_platform == "darwin"

# Premiere-Export: direkte FCPXML-Generierung ist eingebaut,
# OpenTimelineIO ist optionaler Fallback:
# opentimelineio>=0.16

# Optional für Musik-Analyse (Tempo/Energie):
# librosa>=0.10

# Entwicklung / Tests
pytest>=8.0
httpx>=0.27
